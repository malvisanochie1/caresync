// Prisma schema for CareSync.
// Models will be introduced in a later stage.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
